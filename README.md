### Contribution
  - Conventional commits

### Installation
 - `npm i elm`
 - `npm i uglify-js`

### Build
execute `./build.sh`


### Usage
open `index.html` under the `build` directory in the browser
